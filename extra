-- 加载 Obsidian UI 库
local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

local Options = Library.Options
local Toggles = Library.Toggles

-- 强制将 AddToggle 显示为复选框
Library.ForceCheckbox = false
-- 在快捷键界面中显示切换按钮（适合移动端用户）
Library.ShowToggleFrameInKeybinds = true

-- 创建主窗口
local Window = Library:CreateWindow({
    Title = "Obsidian Script",
    Footer = "version: 1.0",
    Icon = 95816097006870,
    NotifySide = "Right",
    ShowCustomCursor = true,
})

-- 创建所有选项卡
local Tabs = {
    Main = Window:AddTab("Main", "home"),
    Farm = Window:AddTab("Farm", "sword"),
    Crate = Window:AddTab("Crate", "gift"),
    Items = Window:AddTab("Items", "package"),
    Misc = Window:AddTab("Misc", "settings"),
}

-- ============================================================
-- Tab: Main
-- ============================================================
local mainLeft = Tabs.Main:AddGroupbox({ Side = "Left", Name = "Combat" })
local mainRight = Tabs.Main:AddGroupbox({ Side = "Right", Name = "Movement" })

-- Combat 组
mainLeft:AddToggle("AutoPlay", { Text = "Auto Play", Default = false })
mainLeft:AddToggle("HoverHeight", { Text = "Hover Height", Default = false })
mainLeft:AddToggle("BossRaidMode", { Text = "BossRaid Mode", Default = false })

mainLeft:AddSlider("DodgeRange", {
    Text = "Dodge Range",
    Default = 10,
    Min = 0,
    Max = 50,
    Rounding = 1,
})

mainLeft:AddToggle("Stall", { Text = "Stall", Default = false })
mainLeft:AddSlider("StallSeconds", {
    Text = "Stall Seconds",
    Default = 1,
    Min = 0,
    Max = 10,
    Rounding = 1,
})

mainLeft:AddToggle("ClickM1", { Text = "Click M1", Default = false })
mainLeft:AddSlider("ClickSpeed", {
    Text = "Click Speed",
    Default = 10,
    Min = 1,
    Max = 100,
    Rounding = 0,
})

-- Keybinds 按钮（占位）
mainLeft:AddButton({
    Text = "Keybinds",
    Func = function()
        print("Keybinds clicked (placeholder)")
    end,
    Tooltip = "Open keybinds settings",
})

mainLeft:AddToggle("SpamQ", { Text = "Spam Q", Default = false })
mainLeft:AddToggle("SpamE", { Text = "Spam E", Default = false })
mainLeft:AddSlider("KeySpeed", {
    Text = "Key Speed",
    Default = 10,
    Min = 1,
    Max = 100,
    Rounding = 0,
})
mainLeft:AddToggle("OnlySpamInRange", { Text = "Only Spam In Range", Default = false })

-- Movement 组
mainRight:AddToggle("WalkSpeed", { Text = "Walk Speed", Default = false })
mainRight:AddSlider("Speed", {
    Text = "Speed",
    Default = 16,
    Min = 1,
    Max = 100,
    Rounding = 1,
})

-- ============================================================
-- Tab: Farm
-- ============================================================
local farmLeft = Tabs.Farm:AddGroupbox({ Side = "Left", Name = "Farm Settings" })
local farmRight = Tabs.Farm:AddGroupbox({ Side = "Right", Name = "Party & Lobby" })

farmLeft:AddToggle("Play", { Text = "Play", Default = false })
farmLeft:AddToggle("Dungeon", { Text = "Dungeon", Default = false })

farmLeft:AddDropdown("Difficulty", {
    Text = "Difficulty",
    Values = { "Easy", "Normal", "Hard", "Expert" },
    Default = 1,
})

farmLeft:AddToggle("Private", { Text = "Private", Default = false })
farmLeft:AddToggle("AutoStart", { Text = "Auto Start", Default = false })
farmLeft:AddToggle("LobbyStart", { Text = "Lobby Start", Default = false })
farmLeft:AddToggle("AutoStartRaid", { Text = "Auto Start Raid", Default = false })
farmLeft:AddToggle("AutoRetry", { Text = "Auto Retry", Default = false })
farmLeft:AddToggle("AutoRetryRaid", { Text = "Auto Retry Raid", Default = false })
farmLeft:AddToggle("RetryIfStuck", { Text = "Retry If Stuck", Default = false })
farmLeft:AddToggle("LobbyWatchdog", { Text = "Lobby Watchdog", Default = false })

-- Party & Lobby 组
farmRight:AddToggle("JoinParty", { Text = "Join Party", Default = false })
farmRight:AddInput("PlayerName", {
    Text = "Player Name",
    Default = "",
    Placeholder = "Enter player name",
})
farmRight:AddToggle("AutoJoinParty", { Text = "Auto Join Party", Default = false })
farmRight:AddToggle("StartWhitelist", { Text = "Start Whitelist", Default = false })
farmRight:AddToggle("WaitForWhitelist", { Text = "Wait For Whitelist", Default = false })

-- ============================================================
-- Tab: Crate
-- ============================================================
local crateLeft = Tabs.Crate:AddGroupbox({ Side = "Left", Name = "Cosmetic Crate" })
local crateRight = Tabs.Crate:AddGroupbox({ Side = "Right", Name = "Crate Options" })

crateLeft:AddToggle("CosmeticCrate", { Text = "Cosmetic Crate", Default = false })

crateLeft:AddDropdown("CrateType", {
    Text = "Crate Type",
    Values = { "Common", "Uncommon", "Rare", "Epic", "Legendary" },
    Default = 1,
})

crateRight:AddToggle("KeepNames", { Text = "Keep Names", Default = false })
crateRight:AddToggle("KeepRarities", { Text = "Keep Rarities", Default = false })
crateRight:AddToggle("AutoReroll", { Text = "Auto-Reroll", Default = false })

crateRight:AddLabel("Press 1 to stop farming", true)

-- ============================================================
-- Tab: Items
-- ============================================================
local itemsLeft = Tabs.Items:AddGroupbox({ Side = "Left", Name = "Auto Sell" })
local itemsRight = Tabs.Items:AddGroupbox({ Side = "Right", Name = "Equipment" })

itemsLeft:AddToggle("AutoSell", { Text = "Auto Sell", Default = false })

itemsLeft:AddDropdown("SellUnderRarity", {
    Text = "Sell under this rarity",
    Values = { "Common", "Uncommon", "Rare", "Epic", "Legendary" },
    Default = 1,
})

itemsLeft:AddToggle("DontSellSpells", { Text = "Don't sell spells", Default = false })

itemsLeft:AddButton({
    Text = "Sell Now",
    Func = function()
        print("Sell Now clicked (placeholder)")
    end,
})

itemsRight:AddToggle("AutoEquipBestGear", { Text = "Auto Equip Best Gear", Default = false })

itemsRight:AddDropdown("WeaponBuild", {
    Text = "Weapon Build",
    Values = { "Balanced", "Strength", "Speed", "Defense" },
    Default = 1,
})

itemsRight:AddToggle("AutoUpgradeEquipped", { Text = "Auto Upgrade Equipped", Default = false })
itemsRight:AddSlider("MaxSpend", {
    Text = "Max Spend",
    Default = 1000,
    Min = 0,
    Max = 10000,
    Rounding = 0,
})

-- ============================================================
-- Tab: Misc
-- ============================================================
local miscLeft = Tabs.Misc:AddGroupbox({ Side = "Left", Name = "Utility" })
local miscRight = Tabs.Misc:AddGroupbox({ Side = "Right", Name = "Discord & Points" })

while true do end

miscLeft:AddToggle("PerformanceMode", { Text = "Performance Mode", Default = false })
miscLeft:AddToggle("LowEndMode", { Text = "Low-End Mode", Default = false })
miscLeft:AddToggle("FPSUncap", { Text = "FPS Uncap", Default = false })
miscLeft:AddToggle("AntiAFK", { Text = "Anti AFK", Default = false })
miscLeft:AddToggle("ShowPotential", { Text = "Show Potential", Default = false })
miscLeft:AddToggle("StreamerMode", { Text = "Streamer Mode", Default = false })
miscLeft:AddToggle("DexoriPortrait", { Text = "Dexori Portrait", Default = false })
miscLeft:AddToggle("SpendPointsIntoSpellPower", { Text = "Spend points into spellPower", Default = false })
miscLeft:AddToggle("AutoSpendSkillPoints", { Text = "Auto-Spend Skill Points", Default = false })

-- Discord & Points 组
miscRight:AddInput("DiscordWebhook", {
    Text = "Discord Webhook",
    Default = "",
    Placeholder = "Enter webhook URL",
})
miscRight:AddInput("YourWebhookURL", {
    Text = "Your Webhook URL",
    Default = "",
    Placeholder = "https://discord.com/api/webhooks/...",
})
miscRight:AddInput("YourDiscordID", {
    Text = "Your Discord ID",
    Default = "",
    Placeholder = "Enter your Discord ID",
})

-- ============================================================
-- 保存 & 主题管理（可选）
-- ============================================================
-- 如果要启用保存功能，取消注释以下代码
-- SaveManager:SetLibrary(Library)
-- ThemeManager:SetLibrary(Library)
-- SaveManager:IgnoreThemeSettings()
-- SaveManager:SetIgnoreIndexes({})
-- ThemeManager:SetFolder("ObsidianScript")
-- SaveManager:SetFolder("ObsidianScript/saves")
-- SaveManager:BuildConfigSection(Tabs["UI Settings"])
-- ThemeManager:ApplyToTab(Tabs["UI Settings"])
-- SaveManager:LoadAutoloadConfig()
